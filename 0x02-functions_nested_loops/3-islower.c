#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _islower(int c)
{
    if (c == 1)
    {
        printf("The character %d is a lowercase character\n", c);
    }
    else
    {
        printf("The character %d is not a lowercase character\n", c);
    }

    return c;
}
