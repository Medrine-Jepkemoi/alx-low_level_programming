#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: Always 0.
 */

int _islower(int c)
{
    int result;
    result = _islower(c);

    if(result == 1)
    {
        return 1;
    }
    else
    {
        return 0;
    }

    return result;
}
